{
  "name": "revapex-events",
  "version": "1.0.0",
  "private": true,
  "description": "events.revapex.ai — static landing page + Netlify Functions backend for Zoho Webinar registration",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test tests/"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
